FastQC was done on Illumina paired-end reads using the module FastQC and using the function fastqc (more information are in the codes section)
Trimming is not necessaryr because we don't have any adapters etc that need to be trimmed. All tests are passed.
